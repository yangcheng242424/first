# 📱 如何在手机上查看效果

## 方法一：通过局域网访问（推荐）

### 前提条件
- 手机和电脑连接到同一个WiFi网络

### 步骤：

1. **确保服务器正在运行**
   ```bash
   # 服务器已在后台运行，端口：8000
   ```

2. **获取电脑的局域网IP地址**
   - Windows: 打开命令提示符，输入 `ipconfig`，查找"IPv4 地址"
   - Mac/Linux: 打开终端，输入 `ifconfig` 或 `ip addr`
   - 通常格式为：`192.168.x.x` 或 `172.16.x.x`

3. **在手机浏览器中访问**
   - 打开手机浏览器（Chrome、Safari等）
   - 输入地址：`http://你的电脑IP:8000`
   - 例如：`http://192.168.1.100:8000`

4. **打开应用**
   - 点击 `index.html` 文件即可查看效果

---

## 方法二：使用ngrok（适合不在同一网络）

### 步骤：

1. **安装ngrok**
   ```bash
   # 访问 https://ngrok.com 注册并下载
   # 或使用包管理器安装
   ```

2. **启动ngrok隧道**
   ```bash
   ngrok http 8000
   ```

3. **获取公网地址**
   - ngrok会显示一个公网URL，例如：`https://xxxxx.ngrok.io`

4. **在手机浏览器访问**
   - 直接访问ngrok提供的URL即可

---

## 方法三：直接传输文件到手机

### 步骤：

1. **将文件打包**
   ```bash
   zip mobile-cursor-demo.zip index.html script.js styles.css
   ```

2. **传输到手机**
   - 通过微信/QQ/邮件发送
   - 通过USB连接传输
   - 通过云盘（百度网盘、iCloud等）

3. **在手机上打开**
   - 解压文件
   - 用手机浏览器打开 `index.html`

---

## 方法四：使用VS Code Live Server（如果使用VS Code）

1. 安装"Live Server"扩展
2. 右键点击 `index.html`
3. 选择"Open with Live Server"
4. 在手机上访问显示的地址

---

## 当前服务器状态

- ✅ Web服务器已启动
- 📍 端口：8000
- 🌐 访问地址：`http://172.30.0.2:8000`（内网地址）

**注意**：如果手机无法访问 `172.30.0.2`，请使用方法一获取您电脑的实际局域网IP地址。

---

## 功能测试清单

在手机上打开后，可以测试以下功能：

- [ ] 代码编辑器是否正常显示
- [ ] 能否输入代码
- [ ] 运行代码功能
- [ ] 保存代码功能
- [ ] 格式化代码功能
- [ ] 语音输入功能（需要浏览器支持）
- [ ] 快捷操作按钮
- [ ] 手势控制（双击、三击等）
- [ ] 控制台输出

---

## 常见问题

**Q: 手机无法访问？**
A: 检查手机和电脑是否在同一WiFi网络，防火墙是否允许8000端口

**Q: 页面显示不正常？**
A: 确保使用现代浏览器（Chrome、Safari、Firefox等）

**Q: 语音输入不工作？**
A: 需要浏览器支持Web Speech API，建议使用Chrome浏览器

**Q: 如何停止服务器？**
A: 在终端按 `Ctrl+C` 或查找进程并终止

---

享受在手机上编程的乐趣！🎉
